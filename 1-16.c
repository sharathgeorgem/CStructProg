#include<stdio.h>
#define MAXLINE 1000

int getline(char line[], int maxline);
void copy(char to[], char from[]);
/*tomorrow on*/
int main() {
	int len;
	int max;
	char line[MAXLINE];
	char longest[MAXLINE];
	/*Many functions left*/
	max = 0;
	while(1){

	}
return 0;
}
